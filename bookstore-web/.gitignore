node_modules/
dist/

# Generated by `specify init` during preparation. Contents drift between
# Spec Kit versions, so they are not committed. The constitution lives at
# ../constitution.md and is copied into .specify/memory/ after init.
.specify/
